# US-027 SAV CHG demo

Open `/?source=manual`, press the wrench (setup), pick X, press ▲ to `SC 5.0`, press ◀ to `SC 1.0`, press ▲ to `SAU ChG`, press ENT → `StorEd` shows; reload the page, re-enter setup → SC reads `SC 1.0` (saved). Repeat but exit via `End` instead of SAV CHG → after reload SC is back to `SC 5.0` (discarded).

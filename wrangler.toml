# Cloudflare Pages project configuration.
# Production: main branch -> https://el400.vza.net
# Previews:   every PR/branch -> https://<branch>.el400.pages.dev (provisioned automatically)
#
# Build settings (set in the Cloudflare dashboard when connecting the repo):
#   Build command:  npm run build
#   Output dir:     dist
#   Root dir:       /
name = "el400"
pages_build_output_dir = "dist"
compatibility_date = "2026-05-30"

/**
 * Shared constants for test utilities
 */

import {
  VALID_NUMBER_PATTERN,
  EXTRACT_NUMBER_FROM_END_PATTERN,
  parseNumericValue,
} from '../../lib/patterns';
export { VALID_NUMBER_PATTERN, EXTRACT_NUMBER_FROM_END_PATTERN, parseNumericValue };
